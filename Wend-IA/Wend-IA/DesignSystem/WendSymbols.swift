import SwiftUI

/// Catálogo de SF Symbols mapeados para o aplicativo Wend.
public enum WendSymbols {
    /// Saudação — manhã (`sun.horizon`)
    public static let greetingMorning = "sun.horizon"

    /// Saudação — tarde (`sun.max`)
    public static let greetingAfternoon = "sun.max"

    /// Saudação — noite (`sun.haze`)
    public static let greetingEvening = "sun.haze"

    /// Saudação — madrugada (`moon.stars`)
    public static let greetingNight = "moon.stars"

    /// Duração/Tempo (`clock`)
    public static let durationClock = "clock"
    
    /// Botão Play/Start (`play.fill`)
    public static let playStart = "play.fill"
    
    /// Exercício Concluído (`checkmark.circle.fill`)
    public static let exerciseCompleted = "checkmark.circle.fill"
    
    /// Exercício Pendente (`clock.fill`)
    public static let exercisePending = "clock.fill"
    
    /// Seta/Navegação (`chevron.right`)
    public static let chevronRight = "chevron.right"
    
    /// Dica do Dia (`lightbulb.fill`)
    public static let tipOfTheDay = "lightbulb.fill"
    
    /// Tab 1 (Exercise) (`figure.flexibility`)
    public static let tabExercise = "figure.flexibility"
    
    /// Tab 1 Alternate (Exercise) (`figure.mind.and.body`)
    public static let tabExerciseAlt = "figure.mind.and.body"
    
    /// Tab 2 (Progress) (`chart.line.uptrend.xyaxis`)
    public static let tabProgress = "chart.line.uptrend.xyaxis"
    
    /// Tab 3 (Profile) (`person.fill`)
    public static let tabProfile = "person.fill"
}
